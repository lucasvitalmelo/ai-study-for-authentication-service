services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: auth_service
      POSTGRES_USER: auth
      POSTGRES_PASSWORD: auth_dev_password
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
